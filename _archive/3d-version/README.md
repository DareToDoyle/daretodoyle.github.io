# Archived 3D version

This folder contains the useful game-specific source from the final React Three Fiber version of Dare to Doyle.

It is retained only as implementation reference and is outside the active `src/` tree, so it is not included in production builds.

The complete working 3D version, including its React shell and package configuration, is also preserved in Git history at commit `d567c55`.
