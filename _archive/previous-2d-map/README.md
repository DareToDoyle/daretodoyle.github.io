# Previous 2D village map

The complete first Phaser village remains available in Git history at commit
`b7c4340` (`Replace 3D world with Phaser pixel village`).

It is not copied into the active source tree because the current map, asset
generators, controls, and UI evolved directly from that implementation. Keeping
the commit reference preserves a clean, useful restore point without bundling
duplicate source or generated assets.
